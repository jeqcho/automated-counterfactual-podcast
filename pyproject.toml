[project]
name = "counterfactual-podcast"
version = "0.1.0"
description = "Rank Trello reading lists by counterfactual impact and produce a TTS listen-queue podcast"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31",
    "trafilatura>=1.8",
    "pypdf>=4.0",
    "anthropic>=0.40",
    "feedgen>=1.0",
    "mutagen>=1.47",
    "boto3>=1.34",
    "python-dotenv>=1.0",
    "numpy>=2.4.6",
    "soundfile>=0.13.1",
    "kokoro-onnx>=0.5.0",
    "pydub>=0.25.1",
    "fastapi>=0.136.3",
    "uvicorn[standard]>=0.48.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23", "responses>=0.25"]
google = [
    "google-cloud-texttospeech>=2.36.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/counterfactual_podcast"]

[tool.pytest.ini_options]
pythonpath = ["src"]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
]
